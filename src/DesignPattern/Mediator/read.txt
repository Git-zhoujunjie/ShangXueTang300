

中介者模式：
 -核心：如果一个系统中对象之间的联系呈现网状结构，对象间存在大量多对多的关系，将导致关系复杂，这些对象称为“同事对象”；
 -因此可以引入一个中介者对象，将复杂的网状结构化解为星型结构

 本质：
  -解耦多个同时对象之间的交互关系。每个对象都持有中介者对象的引用，只跟中介者对象打交道。
   我们通过中介者对象统一管理这些交互关系

 开发场景：
  --MVC模式（其中C，控制器就是一个中介者对象，M和V都要和C打交道）Module、View、Control
  --Java.lang.reflect.Method#invoke()